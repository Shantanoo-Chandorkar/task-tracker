// Fixed, not owner-configurable: owner approval is the real gate, this only bounds a stale invite's lifetime.
export const INVITE_EXPIRY_DAYS = 7;
export const SPACE_INVITES_PER_SENDER_PER_HOUR = 20;
